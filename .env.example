PORT=3000
# Gere uma chave aleatória de 32 bytes em hexadecimal (64 caracteres).
# Exemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=
# Senha do painel. Troque antes de publicar.
ADMIN_PASSWORD=GABRIEL
NIX_API_BASE=https://salas.nixbot.vip